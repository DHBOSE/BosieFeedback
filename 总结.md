# 课堂反馈生成器 · 整体工作总结

> 时间跨度：2026-08-20 ~ 2026-08-27
> 项目位置：`E:\BosieFeedback\webapp\`（可独立推送到 GitHub，README.md 已就位）

---

## 一、项目是什么

面向教培机构 / 独立老师的**本地课堂反馈报告生成工具**：老师粘贴「课堂速览（带时间戳的分段小结）+ 课堂纪要」，AI 归纳成结构化课堂报告，一键产出排版精美的 Word / PDF，附知识点脑图和家长版文字反馈。纯本地运行，支持 DeepSeek / Kimi / 阿里云百炼 / 自定义 OpenAI 兼容端点。

## 二、功能建设全景（按时间线）

### 基础版（8/20 ~ 8/22）
- 报告生成主链路：速览+纪要 → LLM 结构化 JSON → python-docx 排版 → Word / PDF 双格式
- 归纳规则源自 `class-report/SKILL.md`（九大板块、emoji 标题、剔除学科无关内容）
- 多模型供应商架构、VBS 无窗口启动器、历史报告保存

### 品牌与版式（8/23 ~ 8/24 上午）
- 脑图等比缩放：宽度顶满 15.5cm 版心、高度封顶 21.5cm
- 机构品牌设置：封面「Logo + 机构名横排」，页眉/页脚图片+文字（右侧对齐），三槽位独立上传（PNG 透明底/JPG，不限大小）、独立调高度
- 水印设置独立窗口（实时预览）；封面无页眉页脚、页码从正文起算（文档分两节 + pgNumType）
- 批量生成 + ZIP 打包；每节课可单独上传脑图

### AI 脑图（8/24 下午 ~ 深夜）
- 未上传脑图时**第二次独立 LLM 调用**生成 Mermaid 脑图：竖版长图、圆角矩形、固定四级配色
- 提示词工程迭代三轮：语义化层级 id 命名、行内 class 绑定、连线成树校验（N 节点 N-1 边，不合格带反馈重试）
- 打印清晰度组合方案：① 按一级分支间隙**智能切片分页**（viewBox 裁剪，矢量无损）+ ② 布局紧凑化，等效字号 2.4pt → 8.4pt
- 脑图以 **SVG 矢量嵌入 Word**（foreignObject 转原生 text，Word 不支持 foreignObject）

### 体验优化（8/24 ~ 8/26）
- PDF 内嵌预览（iframe inline）+ Word/PDF 切换 + 直接打印；隐藏 PowerShell 黑框；PDF 加载动画
- 全套输入框/按钮换成浮动标签 + 液态玻璃样式；模型选择玻璃下拉（圆角+透明玻璃）
- 学生档案与学情趋势：按姓名自动归档、跨报告 AI 学情总结（依据课堂表现而非错题）、课程范围筛选、学情 PDF 导出；学生姓名必填
- 正文大水印：文字/图片两种，大小/透明度/倾斜角度可调；水印字体 23 种常见中文字体、颜色可调

### 脑图深化 + 家长反馈（8/27）
- 脑图层级自适应：2~5 级按内容自然展开、各分支深度不同、禁止凑三层；样式扩到六级；节点数不设硬上限（实测 28 → 56 节点，内容完整性翻倍）
- 修复文字溢出框线（多行重排）与根节点深底深字（内联 fill 样式）
- 修复推理模型（deepseek-v4-pro）reasoning 耗尽 max_tokens 导致脑图/反馈返回空内容
- **家长反馈 · 简易版**：第三次独立 LLM 调用，固定五板块模板（称呼行/年级科目/授课内容/整体表现⭐️/学习建议/课后作业），可编辑 + 一键复制；批量流程同步支持，ZIP 内附 `-家长反馈.txt`（以弹窗中修改后的版本为准）
- 批量条目支持单独封面信息（折叠四字段，任一非空才传给 AI）

## 三、技术架构

```
浏览器前端（static/index.html，原生 JS + Tailwind 玻璃拟态，docx-preview 渲染 Word 预览）
   │  HTTP（localhost:7100）
Python 后端（server.py，标准库 http.server，零框架）
   ├── llm_client.py      三次独立 LLM 调用：报告 JSON → 脑图 Mermaid → 家长反馈；另有学情总结
   ├── mermaid_render.py  Mermaid → Edge 无头渲染 → 文字化 SVG → 按节点间隙切片 → 3x PNG
   ├── report_builder.py  python-docx 排版：品牌页眉页脚、正文水印（PIL 烘焙浮动图）、SVG 矢量嵌入
   └── pdf_convert.py     docx → pdf：Word COM（保真优先），LibreOffice 兜底
数据：config.json（本地配置，掩码 Key）、brand/（Logo）、runtime/（每次生成一个 job 目录）
```

## 四、关键坑与经验（详见 HANDOVER.md，共 25 条）

1. **服务重启割裂**：前端静态文件按请求从磁盘读、后端逻辑在内存——改了代码不重启会出现「界面是新功能、导出是旧逻辑」（仿宋水印不生效事件）；且 0.0.0.0 与 127.0.0.1 是两个独立 socket 可同时绑定
2. **Mermaid 长文本节点无 `<br/>`**：靠 CSS max-width:200px 视觉换行，转原生 text 必须按框宽重新换行；短标签 foreignObject 宽度恰为文本宽，统一阈值会误拆行
3. **CSS 规则优先级高于 fill 属性**：mermaid 样式表 `#mm .label text{fill:#333}` 会把所有文字压成深灰，文字颜色必须写内联 `style="fill:..."`
4. **推理模型的 reasoning 占 max_tokens**：连「你好」都要 38 tokens 推理，脑图/家长反馈调用 max_tokens 已提到 16000/8000
5. **textarea 的 JS 赋值不进 dump-dom 序列化**：前端验证必须用截图
6. **curl `-d` 直写中文变 GBK**：POST 测试必须 `--data-binary @utf8文件`

## 五、验证方式（每轮改动均实测）

- Python 语法 + 内联 JS（node --check）双检查
- Edge 无头渲染截图目检 UI；PowerPoint COM 渲染验证 Office 同款 SVG 引擎效果
- 全链路解包验证（docx → word/media 提取水印图确认字体字形）
- 真实 LLM 调用实测（脑图 56 节点零溢出、家长反馈六板块齐全、ZIP 附件覆盖逻辑）
- 测试产物全部清理，用户数据（runtime 报告、品牌配置）零污染

## 六、当前状态

- 服务运行于 `http://localhost:7100/`（VBS 启动器，pythonw 无窗口）
- 所有功能已交付并验证；HANDOVER.md 记录全部实现细节与 25 条踩坑
- 仓库已就绪：README.md + .gitignore（隔离 config.json / brand/ / runtime/）+ config.example.json

## 七、清理记录（2026-08-27）

- 移至 `_archive/`：早期的两份手工修订样例报告（docx/pdf）
- 删除：`stitch_ (1)/`（设计草稿，UI 已建成）、`__pycache__/`、`runtime/_mm_live_test/`（测试产物）、`PLAN_学生档案与学情趋势.md`（已实现的计划文档）

## 八、后续可选方向

- 批量弹窗「记住上次填写」，同系列课程连续录入
- 家长反馈语气档位（更正式 / 更亲切）
- 脑图导出单图 PNG 下载
